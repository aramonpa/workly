package com.aramonp.workly.domain

interface AuthRepository {
    /*
    suspend fun signIn(email: String, password: String): User
    suspend fun signUp(email: String, password: String): User
    suspend fun signOut()
    suspend fun getCurrentUser(): User?
    */

}